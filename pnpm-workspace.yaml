packages:
  - "apps/*"
  - "packages/*"

# Force uuid (transitive via expo) past the GHSA-w5hq-g745-h8pq fix.
overrides:
  uuid: ">=11.1.1"

onlyBuiltDependencies:
  - esbuild
allowBuilds:
  esbuild: true
