# Kubernetes

Future manifests and Helm/Kustomize assets will live here when the project moves beyond local Docker and staging prototypes.

Initial priorities before this directory becomes active:

- Container build pipeline.
- Environment variable contract.
- Staging deployment shape.
- Observability baseline.
